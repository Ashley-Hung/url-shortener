<div class="container my-5">
  <h1 class="text-center">URL Shrinker</h1>
  <form action="/shortUrls" method="post" class="my-4 form-inline">
    <label for="fullUrl" class="sr-only">URL</label>
    <input type="url" name="fullUrl" id="fullUrl" placeholder="URL" class="form-control col mr-2" {{#if fullUrl}}value="{{fullUrl}}"{{/if}} required>
    <button type="submit" class="btn btn-primary col-1">Shrink</button>
  </form>

  <div class="form-inline">
    <input type="url" name="shortUrl" id="shortUrl" class="form-control col mr-2" {{#if shortUrl}}value="https://gentle-springs-80779.herokuapp.com/{{shortUrl}}"{{/if}} style="background: white;" readonly>
    <button class="btn btn-outline-success col-1">Copy</button>
  </div>

  <table class="table table-striped table-bordered text-center mt-5">
    <thead>
      <tr>
        <th>Full URL</th>
        <th>Short Url</th>
      </tr>
    </thead>
    {{#each url}}
    <tbody>
      <tr>
        <td><a href="{{this.full}}">{{this.full}}</a></td>
        <td><a href="/{{this.short}}">https://gentle-springs-80779.herokuapp.com/{{this.short}}</a></td>
      </tr>
    </tbody>
    {{/each}}
  </table>
</div>